{
  "commands": [
    "#!/bin/bash",
    "sudo docker stop frontend || true",
    "sudo docker rm frontend || true",
    "sudo docker run --rm -p 80:80 -v /etc/letsencrypt:/etc/letsencrypt -v /var/lib/letsencrypt:/var/lib/letsencrypt certbot/certbot certonly --standalone -d vault-passwordmanager.duckdns.org --non-interactive --agree-tos -m admin@vault.org > /tmp/certbot.log 2>&1",
    "echo \"EXIT: $?\" >> /tmp/certbot.log",
    "cat /tmp/certbot.log"
  ]
}
